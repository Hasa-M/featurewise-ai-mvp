# CLAUDE.md — Featurewise
 
@AGENTS.md
 
All project instructions live in `AGENTS.md` (canonical file, shared with other coding
agents). Treat every rule in it as binding — especially the "Architecture documentation
is the source of truth" section: consult `docs/architecture/adr/` and
`docs/architecture/diagrams/` before implementing, and stop and flag conflicts with
accepted ADRs instead of diverging silently.
 
Do not add project rules here. Edit `AGENTS.md` instead, so Codex and Claude Code
always follow the same instructions.
